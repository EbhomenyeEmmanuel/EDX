/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package edxProjects;
import java.util.Arrays;
/**
 *
 * @author DELL INSPIRON
 */
public class EdxWorkArea {
    public static void main(String[] args) {
       /*
        Maze myMap = new Maze();
        Scanner input = new Scanner(System.in);
        StringBuilder sb = new StringBuilder();
       String direction = " ";
         while(true){
           System.out.print("Where would you like to move?(R, L, U, D ): ");
        direction = input.next();
        direction = direction.charAt(0) + "";
        direction = direction.toUpperCase();
         String [] legalMoves = {
                      "R", "L", "U", "D"
                   };
            if(direction.equals(legalMoves[0]) || direction.equals(legalMoves[1]) || direction.equals(legalMoves[2]) || direction.equals(legalMoves[3])){
                 break;
               // break;
               }
            else{
             System.out.println("Choose a valid direction!!!");
              }              
        }      
       // sb = myMap.printMap();
       return direction;
    }
    public static void legalMove(String direction){
       Maze myMap = new Maze();

        if (direction.equals("R")) {
            if (myMap.canIMoveRight() == true) {
                myMap.MoveRight();
                    // myMap.printMap();
           }else{
                System.out.println("Sorry, you've hit a wall");  
               }
           }
        else if (direction.equals("L")) {
            if (myMap.canIMoveLeft() == true) {
                myMap.MoveLeft();
                        // myMap.printMap();
            }else{
                 System.out.println("Sorry, you've hit a wall");  
                }
            }
        else if (direction.equals("U")) {
             if (myMap.canIMoveUp() == true) {
                        myMap.MoveUp();
                        //myMap.printMap();
                }else{
                     System.out.println("Sorry, you've hit a wall");  
                    }
            }
               else if (direction.equals("D")) {
                    if (myMap.canIMoveDown() == true) {
                        myMap.MoveDown();
                        //myMap.printMap();
                    }else{
                        System.out.println("Sorry, you've hit a wall");  
                    }
         }
    }
        */
    }
}
